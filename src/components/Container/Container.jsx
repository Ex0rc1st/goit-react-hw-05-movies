import { color, space, layout, shadow, border, flexbox } from 'styled-system';
import styled from 'styled-components';

const Container = styled('div')(color, space, layout, shadow, border, flexbox);

export default Container;
